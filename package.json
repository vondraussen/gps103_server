{
  "name": "gps103-server",
  "version": "1.0.0",
  "author": "Andy Hempel",
  "license": "ISC",
  "description": "Server/Gateway to receive and response to gps103 GPS Tracker messages and forward the parsed content via MQTT",
  "scripts": {
    "start": "node app.js",
    "test": "jest",
    "debug": "node --debug-brk --inspect ./node_modules/jest/bin/jest -i"
  },
  "keywords": [
    "GPS",
    "GPS103",
    "Tracker",
    "MQTT",
    "Gateway"
  ],
  "devDependencies": {
    "jest": "^24.8.0"
  },
  "dependencies": {
    "dotenv": "^8.0.0",
    "mqtt": "^3.0.0"
  }
}